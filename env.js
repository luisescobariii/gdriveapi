/**
 * @namespace
 * @property {object} auth
 * @property {string} auth.id - Id of the authorization spreadsheet.
 * @property {string} auth.users - Name of the user's sheet.
 */
const env = {
    auth: {
        id: '',
        users: ''
    }
};